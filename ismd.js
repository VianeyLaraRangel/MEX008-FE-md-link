const path = require('path');

const isMarkdown = (path) => {
path
};

module.exports = isMarkdown;
//Hacer require del file system "fs" de node.js
//Hacer require del "node fetch" de node
//Usar process.argv de node para leer el path cuando se reciba

//Hacer un modulo "is markdown" para detectar si hay un archivo md

//Hacer un modulo "filterLinks" y usando expresiones regulares detectarlo

//Hacer un módulo que guarde en un arreglo para que extraiga con los tokens con path.extname de node luego debe leer con fs.readFile

//