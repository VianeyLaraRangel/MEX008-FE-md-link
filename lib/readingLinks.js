//Hacer un módulo que guarde en un arreglo para que extraiga con los tokens con path.extname de node luego debe leer con fs.readFile

const readFile = (fs) => {
console.log('Conectado');
};

module.exports = readFile;